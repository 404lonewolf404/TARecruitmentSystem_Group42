package com.bupt.tarecruitment.model;

/**
 * 职位状态枚举
 * 定义职位的两种状态
 */
public enum PositionStatus {
    /**
     * 开放 - 职位可以接受申请
     */
    OPEN,
    
    /**
     * 关闭 - 职位不再接受申请
     */
    CLOSED
}
